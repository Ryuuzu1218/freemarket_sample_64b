.card-content
  %h2.card-content__title
    支払い方法
  .card-content__main
    .card-title
      %h3.card-title__letter クレジットカード
    .card-body
      .card-body__left
        .card-image
          = image_tag @card_src, width:'49px', height: '20px', class: 'visa'
        .card-number
          **** **** **** #{@customer_card.last4}
        .card-deadline
          #{@customer_card.exp_month.to_s} / #{@customer_card.exp_year.to_s.slice(2,3)}
      .card-body__right
        = link_to card_path, method: :delete, class: "card-remove" do
          削除する



